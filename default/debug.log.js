module.exports = 







    // debug that can be used in spawn/added on
    /*
    if (debugerrorspawn = 1) {
        console.log('! We are getting a spawn error!')
        console.log('! Please define Memory.creepSpawn')
        console.log('! ---------------------------------- ')
    }
    
    if (Memory.debug.error = undefined) {
        Memory.debug.error = false;
    }
    
    
    // A spammy error code :D
    if (debugerror = true) {
    
        for (let i = 0; i < 200; i++) {
            console.log('Getting an Error!')
        }
    
    
    }*/
    
    
       // We should make a .prototype so we can toggle specific debug outputs : D
        
