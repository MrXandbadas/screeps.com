
var roleHarvester = require('role.harvester');
var roleUpgrader = require('role.upgrader');
var roleBuilder = require('role.builder');
var roleRepairer = require('role.repairer');
var main = require('main');

// define spawning var

var spawningharvester = 'false';
var spawningupgrader = 'false';
var spawningrepairer = 'false';
var spawningbuilder = 'false';

var roles = {
    harvester: require('role.harvester'),
    upgrader: require('role.upgrader'),
    builder: require('role.builder'),
    repairer: require('role.repairer'),
    wallRepairer: require('role.wallRepairer'),
    longDistanceHarvester: require('role.longDistanceHarvester'),
    claimer: require('role.claimer'),
    miner: require('role.miner'),

};


//Imported modules above this line only.


var functioncreep = {






    run: function (creepnamecheck) {
        var creepnamecheck = 'true';

        if (creepnamecheck == 'true') {


        
                console.log('checkdone');

                console.log('Harvester: ' + harvestcount.length);
                console.log('Builders: ' + buildercount.length);
                console.log('Repairer: ' + repairercount.length);
                console.log('Upgrader: ' + upgradercount.length);
                var checkdone = 1 - 1;
                console.log(checkdone);
            creepnamecheck = 'false';
        }

    },




    //

    run: function (AlphaHarvest1Spawn) {



        var harvest1 = {
            if(spawningharvester = 'false') {
                functioncreep.run(creepnamecheck);
                console.log('False!');
            }

	        //
	        if(spawningharvester = 'true') {

                Game.spawns['SpawnAlpha'].spawnCreep([WORK, WORK, CARRY, MOVE], newNameHarvest,
                { memory: { role: 'harvester' } });
            console.log('Order for ' + NewCreepName);

                }
        }
                }
            }   

          


// Start of If functions
